<h1>Follows</h1>

<table class="table">
<% @users.each do |user| %>
  <tr>
    <th>Image</th>
    <th>name</th>
    <th></th>
    <th></th>
  </tr>
  <tr>
    <td><%= attachment_image_tag user, :profile_image, size:"30x30", format: 'jpeg', fallback: "no_image.jpg" %></td>
    <td><%= user.name %></td>
  </tr>